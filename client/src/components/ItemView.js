import React from 'react'

const ItemView = (props) => {

    return (

        <div>
            {/* {props.items.map(item => (
                <div>{item.name}</div>
            ))} */}
            Item View Component
        </div>

    )

}

export default ItemView